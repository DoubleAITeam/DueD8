# API Reference

_Last generated: 2025-10-04T19:07:46.508Z_

> ⚠️ This file is auto-generated by `npm run docs:api`. Do not edit by hand.

## electron/deliverables/aiSummary.ts

### `buildAiSummary`

*Kind*: function

```ts
function buildAiSummary(run: DeliverableRunRecord, base: RunSummary): Promise<string | null>
```

## electron/deliverables/archive.ts

### `zipArtifacts`

*Kind*: function

```ts
function zipArtifacts(artifactIds: string[], runId: string): Promise<ZipResult>
```

### `zipRun`

*Kind*: function

```ts
function zipRun(runId: string): Promise<ZipResult>
```

## electron/deliverables/cli.ts

### `runCLI`

*Kind*: function

```ts
function runCLI(rawArgs: string[]): Promise<number>
```

## electron/deliverables/config.ts

### `getConfigIssues`

*Kind*: function

```ts
function getConfigIssues(): ConfigValidationIssue[]
```

### `getDeliverablesConfig`

*Kind*: function

```ts
function getDeliverablesConfig(): DeliverablesConfig
```

### `getMaxFileMb`

*Kind*: function

```ts
function getMaxFileMb(): number
```

### `getProtectRecentRuns`

*Kind*: function

```ts
function getProtectRecentRuns(): number
```

### `getRenderFlags`

*Kind*: function

```ts
function getRenderFlags(): RenderFeatureFlags
```

### `getRetentionDays`

*Kind*: function

```ts
function getRetentionDays(): number
```

### `getTimeoutMs`

*Kind*: function

```ts
function getTimeoutMs(): number
```

### `isAutoArchiveEnabled`

*Kind*: function

```ts
function isAutoArchiveEnabled(): boolean
```

### `isHardDeleteEnabled`

*Kind*: function

```ts
function isHardDeleteEnabled(): boolean
```

### `RenderFeatureFlags`

*Kind*: interface

```ts
export interface RenderFeatureFlags {
    html_puppeteer?: boolean;
    docx_libreoffice?: boolean;
    pdf_qpdf?: boolean;
}
```

### `resetDeliverablesConfigCache`

*Kind*: function

```ts
function resetDeliverablesConfigCache(): void
```

## electron/deliverables/config/validate.ts

### `ConfigValidationIssue`

*Kind*: interface

```ts
export interface ConfigValidationIssue {
    code: IssueCode;
    field: string;
    message: string;
    value?: string;
}
```

### `ConfigValidationResult`

*Kind*: interface

```ts
export interface ConfigValidationResult {
    config: DeliverablesConfig;
    issues: ConfigValidationIssue[];
}
```

### `DeliverablesConfig`

*Kind*: interface

```ts
export interface DeliverablesConfig {
    timeouts: {
        runMs: number;
        aiInsightsMs: number;
        aiSummaryMs: number;
    };
    sizeCaps: {
        maxArtifactMb: number;
    };
    concurrency: {
        maxParallelArtifacts: number;
    };
    retention: {
        days: number;
        protectRecentRuns: number;
        hardDelete: boolean;
        autoArchive: boolean;
    };
    logs: {
        memoryEntries: number;
    };
    featureFlags: {
        htmlPuppeteer: boolean;
        docxLibreoffice: boolean;
        pdfQpdf: boolean;
        aiSummary: boolean;
        aiInsights: boolean;
    };
    insights: {
        maxPages: number;
        maxBytesMb: number;
        maxBytes: number;
        redact: boolean;
        aiTimeoutMs: number;
    };
    ai: {
        openAiApiKey: string | null;
    };
    os: {
        allowExternalMove: boolean;
    };
    issues: ConfigValidationIssue[];
}
```

### `loadConfig`

*Kind*: function

```ts
function loadConfig(env?: EnvSource): ConfigValidationResult
```

## electron/deliverables/dataStore.ts

### `getRun`

*Kind*: function

```ts
function getRun(runId: string): Promise<DeliverableRunRecord | null>
```

### `getRuns`

*Kind*: function

```ts
function getRuns(limit?: number | undefined): Promise<DeliverableRunRecord[]>
```

### `getSummary`

*Kind*: function

```ts
function getSummary(runId: string): Promise<RunSummary | null>
```

### `resetRuns`

*Kind*: function

```ts
function resetRuns(): Promise<void>
```

### `saveRun`

*Kind*: function

```ts
function saveRun(record: DeliverableRunRecord): Promise<void>
```

### `saveSummary`

*Kind*: function

```ts
function saveSummary(runId: string, summary: RunSummary): Promise<void>
```

## electron/deliverables/insights/build.ts

### `buildAiInsights`

*Kind*: function

```ts
function buildAiInsights(runId: string, bundle: InsightBundle): Promise<InsightBundle>
```

### `buildBaseInsights`

*Kind*: function

```ts
function buildBaseInsights(run: DeliverableRunRecord, opts?: { limit?: number | undefined; } | undefined): Promise<InsightBundle>
```

### `getRedactionInfo`

*Kind*: function

```ts
function getRedactionInfo(): { enabled: boolean; patterns: string[]; }
```

### `isAiInsightsAvailable`

*Kind*: function

```ts
function isAiInsightsAvailable(): boolean
```

### `loadInsightBundle`

*Kind*: function

```ts
function loadInsightBundle(runId: string): Promise<InsightBundle | null>
```

### `saveInsightCorrection`

*Kind*: function

```ts
function saveInsightCorrection(runId: string, artifactId: string, patch: Partial<Pick<BaseInsight, "title" | "detectedCourseId" | "detectedAssignmentId">>): Promise<InsightBundle | null>
```

## electron/deliverables/insights/config.ts

### `getAiInsightsTimeoutMs`

*Kind*: function

```ts
function getAiInsightsTimeoutMs(): number
```

### `getInsightsMaxBytes`

*Kind*: function

```ts
function getInsightsMaxBytes(): number
```

### `getInsightsMaxBytesMb`

*Kind*: function

```ts
function getInsightsMaxBytesMb(): number
```

### `getInsightsMaxPages`

*Kind*: function

```ts
function getInsightsMaxPages(): number
```

### `getOpenAiApiKey`

*Kind*: function

```ts
function getOpenAiApiKey(): string | undefined
```

### `isAiInsightsEnabled`

*Kind*: function

```ts
function isAiInsightsEnabled(): boolean
```

### `isRedactionEnabled`

*Kind*: function

```ts
function isRedactionEnabled(): boolean
```

## electron/deliverables/insights/parsers.ts

### `computeKeywords`

*Kind*: function

```ts
function computeKeywords(text: string | undefined, limit?: number): string[] | undefined
```

### `countWords`

*Kind*: function

```ts
function countWords(text: string | undefined): number | undefined
```

### `parseArtifact`

*Kind*: function

```ts
function parseArtifact(filePath: string, kind: ArtifactKind): Promise<ParserResult>
```

### `ParserResult`

*Kind*: interface

```ts
export interface ParserResult {
    text?: string;
    title?: string;
    author?: string;
    detectedCourseId?: string;
    detectedAssignmentId?: string;
    pageCount?: number;
    warnings: string[];
}
```

## electron/deliverables/insights/redact.ts

### `getRedactionPatterns`

*Kind*: function

```ts
function getRedactionPatterns(): string[]
```

### `maybeRedactText`

*Kind*: function

```ts
function maybeRedactText(raw: string | undefined): { text?: string | undefined; redacted: boolean; }
```

### `redactText`

*Kind*: function

```ts
function redactText(raw: string): { text: string; redacted: boolean; }
```

## electron/deliverables/insights/types.ts

### `AiInsight`

*Kind*: interface

```ts
export interface AiInsight {
    artifactId: string;
    summary?: string; // ≤ 1200 chars
    actionItems?: string[]; // short bullets
    confidence?: number; // 0..1
    model?: string;
}
```

### `BaseInsight`

*Kind*: interface

```ts
export interface BaseInsight {
    artifactId: string;
    kind: ArtifactKind;
    title?: string;
    author?: string;
    detectedCourseId?: string;
    detectedAssignmentId?: string;
    wordCount?: number;
    pageCount?: number;
    keywords?: string[];
    warnings?: string[]; // size > limit, unreadable, etc.
    redacted?: boolean;
}
```

### `InsightBundle`

*Kind*: interface

```ts
export interface InsightBundle {
    runId: string;
    createdAt: number;
    base: Record<string, BaseInsight>; // by artifactId
    ai?: Record<string, AiInsight>; // optional
    version: number; // bump on schema change
}
```

## electron/deliverables/jobs/renderArtifactsJob.ts

### `run`

*Kind*: function

```ts
function run(artifact: ArtifactInput, runId: string): Promise<DeliverableJobResult>
```

## electron/deliverables/jobs/validateArtifact.ts

### `run`

*Kind*: function

```ts
function run(artifact?: ArtifactInput | undefined): Promise<DeliverableJobResult>
```

## electron/deliverables/logStore.ts

### `appendLog`

*Kind*: function

```ts
function appendLog(entry: Omit<DeliverableLogEntry, "timestamp"> & { timestamp?: number | undefined; }): void
```

### `clearLogs`

*Kind*: function

```ts
function clearLogs(): void
```

### `getLogs`

*Kind*: function

```ts
function getLogs(): DeliverableLogEntry[]
```

## electron/deliverables/osActions.ts

### `moveToTrash`

*Kind*: function

```ts
function moveToTrash(p: string): Promise<{ ok: boolean; message?: string | undefined; }>
```

### `openPath`

*Kind*: function

```ts
function openPath(p: string): Promise<{ ok: boolean; message?: string | undefined; }>
```

### `revealInFolder`

*Kind*: function

```ts
function revealInFolder(p: string): Promise<boolean>
```

## electron/deliverables/pipeline.ts

### `__determineBadgeForTest`

*Kind*: function

```ts
function __determineBadgeForTest(result: AnnotatedResult): BadgeKind
```

### `runDeliverablePipeline`

*Kind*: function

```ts
function runDeliverablePipeline(artifacts?: ArtifactInput[], options?: { concurrency?: number | undefined; dryRun?: boolean | undefined; post?: { enable?: boolean | undefined; dryRun?: boolean | undefined; ctx?: CourseContext | undefined; } | undefined; } | undefined): Promise<...>
```

## electron/deliverables/postprocess/config.ts

### `getDefaultRules`

*Kind*: function

```ts
function getDefaultRules(_ctx?: CourseContext | undefined): Rule[]
```

### `getRules`

*Kind*: function

```ts
function getRules(): Promise<Rule[]>
```

### `resetRules`

*Kind*: function

```ts
function resetRules(): Promise<void>
```

### `setRules`

*Kind*: function

```ts
function setRules(rules: Rule[]): Promise<void>
```

## electron/deliverables/postprocess/engine.ts

### `applyActions`

*Kind*: function

```ts
function applyActions(result: DeliverableJobResult, course: CourseContext, actions: PostAction[], opts?: ApplyOptions): Promise<PostResult>
```

### `matchRule`

*Kind*: function

```ts
function matchRule(result: DeliverableJobResult, _ctx: CourseContext, rule: Rule): boolean
```

## electron/deliverables/postprocess/template.ts

### `applyTemplate`

*Kind*: function

```ts
function applyTemplate(template: string, context: TemplateContext): string
```

### `normalizeTemplatedPath`

*Kind*: function

```ts
function normalizeTemplatedPath(input: string): string
```

### `TemplateContext`

*Kind*: interface

```ts
export interface TemplateContext {
    result: Partial<DeliverableJobResult> & {
        id: string;
    };
    course?: CourseContext;
    runId?: string;
    timestamp?: string;
}
```

## electron/deliverables/postprocess/types.ts

### `CourseContext`

*Kind*: interface

```ts
export interface CourseContext {
    courseId?: string;
    courseName?: string;
    assignmentId?: string;
    dueDateIso?: string;
    meta?: Record<string, string | number | boolean>;
}
```

### `PostResult`

*Kind*: interface

```ts
export interface PostResult {
    artifactId: string;
    appliedRuleIds: string[];
    outputs: {
        from: string;
        to?: string;
        action: string;
        ok: boolean;
        message?: string;
    }[];
}
```

### `Rule`

*Kind*: interface

```ts
export interface Rule {
    id: string;
    description?: string;
    enabled: boolean;
    when: RuleCondition;
    actions: PostAction[];
}
```

### `RuleCondition`

*Kind*: interface

```ts
export interface RuleCondition {
    type?: ArtifactKind[];
    minSizeMb?: number;
    maxSizeMb?: number;
    adapterId?: string[];
    badge?: ('success' | 'warning' | 'error' | 'cancelled' | 'skipped')[];
    pathIncludes?: string[];
}
```

## electron/deliverables/renderers/adapter.ts

### `AdapterHealth`

*Kind*: interface

```ts
export interface AdapterHealth {
    ok: boolean;
    message?: string;
    details?: Record<string, unknown>;
}
```

### `RendererAdapter`

*Kind*: interface

```ts
export interface RendererAdapter {
    id: string;
    handles: ('pdf' | 'docx' | 'html')[];
    health(): Promise<AdapterHealth>;
    render(args: {
        artifact: ArtifactInput;
        outDir: string;
        signal: AbortSignal;
        dryRun?: boolean;
    }): Promise<DeliverableJobResult>;
}
```

## electron/deliverables/renderers/registry.ts

### `__resetRegistryForTests`

*Kind*: function

```ts
function __resetRegistryForTests(): void
```

### `__selectAdapterForTests`

*Kind*: function

```ts
function __selectAdapterForTests(kind: ArtifactKind): Promise<AdapterSelection>
```

### `AdapterOverviewEntry`

*Kind*: interface

```ts
export interface AdapterOverviewEntry {
    selectedAdapterId: string;
    selectedHealth: AdapterHealth;
    preferredAdapterId: string | null;
    preferredHealth: AdapterHealth | null;
    fallbackAdapterId: string | null;
    fallbackHealth: AdapterHealth | null;
    usingFallback: boolean;
    reason: string;
}
```

### `AdapterSelection`

*Kind*: interface

```ts
export interface AdapterSelection {
    adapter: RendererAdapter;
    health: AdapterHealth;
    usedFallback: boolean;
    reason: string;
    preferredId?: string | null;
    preferredHealth?: AdapterHealth | null;
    fallbackId?: string | null;
    fallbackHealth?: AdapterHealth | null;
    fallbackAdapter?: RendererAdapter | null;
}
```

### `getAdapterOverview`

*Kind*: function

```ts
function getAdapterOverview(): Promise<Record<ArtifactKind, AdapterOverviewEntry>>
```

### `getAdapterSelection`

*Kind*: function

```ts
function getAdapterSelection(kind: ArtifactKind): Promise<AdapterSelection>
```

### `getBestAdapter`

*Kind*: function

```ts
function getBestAdapter(kind: ArtifactKind): Promise<RendererAdapter>
```

### `refreshAdapterHealth`

*Kind*: function

```ts
function refreshAdapterHealth(adapterId?: string | undefined): Promise<void>
```

### `register`

*Kind*: function

```ts
function register(adapter: RendererAdapter): void
```

## electron/deliverables/renderers/staticHtml.ts

### `sanitizeHtml`

*Kind*: function

```ts
function sanitizeHtml(input: string): string
```

## electron/deliverables/report.ts

### `writeRunReport`

*Kind*: function

```ts
function writeRunReport(run: DeliverableRunRecord, summary: RunSummary, opts?: { format?: "html" | "json" | undefined; } | undefined): Promise<string>
```

## electron/deliverables/retention.ts

### `RetentionConfig`

*Kind*: interface

```ts
export interface RetentionConfig {
    keepDays?: number;
    maxArchives?: number;
    dryRun?: boolean;
}
```

### `sweepOldOutputs`

*Kind*: function

```ts
function sweepOldOutputs(cfg?: RetentionConfig | undefined): Promise<SweepResult>
```

## electron/deliverables/summary.ts

### `buildRunSummary`

*Kind*: function

```ts
function buildRunSummary(run: DeliverableRunRecord): RunSummary
```

## electron/deliverables/types.ts

### `ArtifactInput`

*Kind*: interface

```ts
export interface ArtifactInput {
    id: string;
    srcPath: string;
    type: ArtifactKind;
}
```

### `DeliverableJobResult`

*Kind*: interface

```ts
export interface DeliverableJobResult {
    id: string;
    success: boolean;
    message?: string;
    outputPath?: string;
    errors?: ValidationError[];
    adapterId?: string;
    adapterReason?: string;
    attempted?: string[];
    attempts?: number;
    transientFailureCodes?: string[];
    transientErrorCode?: string;
    dryRun?: boolean;
    type?: ArtifactKind;
    badge?: BadgeKind;
}
```

### `DeliverableLogEntry`

*Kind*: interface

```ts
export interface DeliverableLogEntry {
    timestamp: number;
    level: DeliverableLogLevel;
    scope: string;
    message: string;
    data?: Record<string, unknown>;
}
```

### `DeliverableRunRecord`

*Kind*: interface

```ts
export interface DeliverableRunRecord {
    runId: string;
    startedAt: number;
    finishedAt: number;
    results: DeliverableJobResult[];
    options?: {
        dryRun?: boolean;
        concurrency?: number;
        post?: {
            enable?: boolean;
            dryRun?: boolean;
        };
    };
    post?: {
        success: boolean;
        results: PostResult[];
        ctx?: CourseContext;
        dryRun?: boolean;
    };
}
```

### `RunSummary`

*Kind*: interface

```ts
export interface RunSummary {
    runId: string;
    startedAt: number;
    finishedAt: number;
    totals: {
        count: number;
        ok: number;
        failed: number;
        cancelled: number;
        skipped: number;
    };
    byType: Record<ArtifactKind, {
        count: number;
        ok: number;
        failed: number;
    }>;
    adaptersUsed: Record<string, number>;
    fallbackCount: number;
    durationMs: number;
    headline: string;
    bullets: string[];
}
```

### `ValidationError`

*Kind*: interface

```ts
export interface ValidationError {
    code: 'MISSING_FILE' | 'UNSUPPORTED_TYPE' | 'EMPTY_FILE' | 'SIZE_LIMIT';
    details?: string;
}
```

## electron/deliverables/utils/retry.ts

### `isTransientError`

*Kind*: function

```ts
function isTransientError(error: unknown): boolean
```

### `RetryOptions`

*Kind*: interface

```ts
export interface RetryOptions {
    tries?: number;
    baseMs?: number;
    onRetry?: (error: unknown, attempt: number, remaining: number) => void;
}
```

### `withRetry`

*Kind*: function

```ts
function withRetry<T>(fn: () => Promise<T>, opts?: RetryOptions): Promise<T>
```

## electron/deliverables/utils/tools.ts

### `SpawnResult`

*Kind*: interface

```ts
export interface SpawnResult {
    code: number | null;
    stdout: string;
    stderr: string;
}
```

### `spawnWithLimits`

*Kind*: function

```ts
function spawnWithLimits(command: string, args?: string[], options?: SpawnWithLimitsOptions): Promise<SpawnResult>
```

### `SpawnWithLimitsOptions`

*Kind*: interface

```ts
export interface SpawnWithLimitsOptions extends SpawnOptions {
    timeoutMs?: number;
    maxStdoutBytes?: number;
    maxStderrBytes?: number;
    signal?: AbortSignal;
}
```

### `which`

*Kind*: function

```ts
function which(cmd: string): Promise<string | null>
```
